define([
    'require',
    './common',
    'app/math',
    'app/def-obj',
    'app/def-function-common',
    // 'app/resp.json?callback=define'
], function(require, common, math, obj, ca) {
    'use strict';
    
    console.log(math)
    console.log(obj)
    console.log(ca)
});